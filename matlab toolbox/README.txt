Run processAllXML(folder) function to process all annotation XMLs to label indexed text files
Run visualiseXMLLabels script to visualise an annotation in MATLAB. Enter the script and change respective parameters accordingly.
